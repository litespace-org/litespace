{
  "name": "@litespace/kafka",
  "version": "1.0.0",
  "description": "LiteSpace centralized kafka setup",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "",
    "prepare": "ts-patch install -s",
    "check": "tsc -p tsconfig.json --noEmit",
    "watch": "npm-watch build",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "@litespace/types": "workspace:^",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "ts-patch": "^3.2.1",
    "typescript": "^5.4.5",
    "typescript-eslint": "^8.0.1",
    "typescript-transform-paths": "^3.5.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
