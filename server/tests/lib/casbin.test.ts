// import { expect, describe, it } from "@jest/globals";
import { expect } from "chai";
import fs from "node:fs";

describe("Cabin", () => {
  it("should work", () => {
    expect(1).to.be.eq(1);
  });
});
