{
  "name": "litespace",
  "version": "1.0.0",
  "description": "Online English classes",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "install:clean": "yarn workspaces run rimraf node_modules && yarn rimraf node_modules && yarn install"
  },
  "workspaces": {
    "packages": [
      "server",
      "client",
      "components"
    ]
  }
}
